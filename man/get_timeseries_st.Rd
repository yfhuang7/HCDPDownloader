% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_timeseries_st.R
\name{get_timeseries_st}
\alias{get_timeseries_st}
\title{Download Station Timeseries Data from the HCDP API}
\usage{
get_timeseries_st(
  station_id,
  datatype,
  period,
  fill,
  start_date,
  end_date,
  token,
  production = NULL,
  aggregation = NULL,
  limit = 10000,
  offset = 0
)
}
\arguments{
\item{station_id}{The station ID for which data is to be retrieved (e.g., "1094.2").}

\item{datatype}{The type of data to retrieve (e.g., "rainfall" or "temperature").}

\item{period}{The time period for the data (e.g., "month", "day").}

\item{fill}{The fill type for the data (e.g., "partial", "complete").}

\item{start_date}{Start date for the data query (format: "YYYY-MM-DD").}

\item{end_date}{End date for the data query (format: "YYYY-MM-DD").}

\item{token}{Your personal access token for authentication.}

\item{production}{Optional. Specifies data production type if required (e.g., "new").}

\item{aggregation}{Optional. Specifies the aggregation method if applicable.}

\item{limit}{Number of records to return (default: 10000).}

\item{offset}{Offset in the records returned (default: 0).}
}
\value{
A data frame containing the station timeseries data.
}
\description{
This function queries the HCDP API to retrieve station timeseries data (e.g., rainfall or temperature) for a specific station.
}
\examples{
\dontrun{
  download_station_timeseries(
    station_id = "1094.2",
    datatype = "rainfall",
    period = "month",
    fill = "partial",
    start_date = "1994-01-01",
    end_date = "1995-01-01",
    token = "YOUR_ACCESS_TOKEN"
  )
}
}
